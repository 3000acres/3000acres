%title
  - if ! cms_block_content(:title).blank?
    = cms_block_content(:title)
    \:
    = ENV['acres_site_name']
  - elsif content_for?(:title)
    = yield(:title)
    \:
    = ENV['acres_site_name']
  - else
    = ENV['acres_site_name']
